{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "discord-store-bot.fullname" . }}-api
  labels:
    {{- include "discord-store-bot.labels" . | nindent 4 }}
    component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
  - port: {{ .Values.api.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "discord-store-bot.selectorLabels" . | nindent 4 }}
    component: api
{{- end }}
